# Web app (Next.js) environment variables

# Backend API origin (NestJS). The /api/backend/* rewrite proxies to this.
API_URL=http://localhost:4000

# Public store name shown in header/footer/SEO. Safe to expose (NEXT_PUBLIC_).
NEXT_PUBLIC_STORE_NAME=test store
